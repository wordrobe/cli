<?php

namespace {NAMESPACE}\DTO;

use Wordrobe\Feature\DTOInterface;
use {NAMESPACE}\Entity\Entity;

class DTO implements DTOInterface
{
  // properties here
  // e.g. private $title;

  /**
   * DTO's protected constructor
   * @param \{NAMESPACE}\Entity\Entity $entity
   */
  public function __construct(Entity $entity)
  {
    // setting data here
    // e.g. $this->title = $entity->getTitle();
  }

  /**
   * DTO's data getter
   */
  public function getData()
  {
    return (array) $this; // or map data like you want
  }
}
