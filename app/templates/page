<?php

/*
 * Template Name: {TEMPLATE_NAME}
 */

use Timber\Timber;
use {NAMESPACE}\Handler\{ENTITY_NAME}Handler;

$context = Timber::get_context();
$context['contents'] = {ENTITY_NAME}Handler::getPostDTO();

Timber::render('templates/pages/{VIEW_FILENAME}.html.twig', $context);
