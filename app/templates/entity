<?php

namespace {NAMESPACE}\Entity;

use Wordrobe\Feature\EntityInterface;

/**
 * Class Entity
 * @package {NAMESPACE}\Entity
 */
class Entity implements EntityInterface
{
  /**
   * @var string $title
   */
  private $title;

  /**
   * @var string $url
   */
  private $url;

  /**
   * Entity's protected constructor
   * @param string $title
   * @param string $url
   * @param mixed ...$params
   */
  public function __construct($title, $url, ...$params)
  {
    $this->title = $title;
    $this->url = $url;
  }

  /**
   * Entity's title getter
   */
  public function getTitle()
  {
    return $this->title;
  }

  /**
   * Entity's url getter
   */
  public function getUrl()
  {
    return $this->url;
  }
}
