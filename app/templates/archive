<?php

use Timber\Timber;
use {NAMESPACE}\Handler\{ENTITY_NAME}Handler;

$context = Timber::get_context();
$context['posts'] = {ENTITY_NAME}Handler::getPostsDTO();

Timber::render('templates/default/{VIEW_FILENAME}.html.twig', $context);
