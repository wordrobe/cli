<?php

namespace {NAMESPACE}\DTO;

use Wordrobe\Feature\DTOInterface;
use {NAMESPACE}\Entity\{ENTITY_NAME};

class {ENTITY_NAME}DTO extends DTO
{
  // extend properties here
  // e.g. private $my_custom_prop;

  /**
   * {ENTITY_NAME}DTO constructor
   * @param \{NAMESPACE}\Entity\{ENTITY_NAME} $entity
   */
  public function __construct({ENTITY_NAME} $entity)
  {
    parent::__construct($entity);

    // extend data here
    // e.g. $this->my_custom_prop = $entity->getMyCustomProp();
  }

  // (if needed) override methods here
}
